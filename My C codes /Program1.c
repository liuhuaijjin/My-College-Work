#include <stdio.h>

int a[10] = {12,14,27,33,26,17,12,7}
int i,j=0;
int high = 9; , low = 0;
while high!=low
{
int mid = (high+low)/2;
if
(a[mid+1]>a[mid] && a[mid]>a[mid-1];
low = mid;
else if
(a[mid+1]<a[mid] && a[mid]<[mid-1];
high = mid;
else if
(a[mid+1]<a[mid] && a[mid]>[mid-1];
